#! /usr/bin/env bash

#
# You can run your own code in here!
# The script is executed whenever a project was compiled and contains at least one call to a specified package.
#

# parameter: $1=absolute_repo_path, $2=absolute_project_path

exit(0);